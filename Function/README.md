# Function Layer

This folder contains business and update logic required by the CIMC contest structure.

- `boot/`: Bootloader flash map, parameter block, image validation, image copy, and application jump support.
- `app/`: Application-side firmware staging support.

Future contest features such as sampling, ratio/threshold management, alarms, parameter persistence, and auto reporting should be added here.
